@{
    PrivateData = @{
        PSData = @{
            Tags = @(
                'JWT'
                'JSON'
                'Token'
                'Authentication'
                'Security'
                'PSModule'
            )
        }
    }
}
