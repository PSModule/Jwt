@{
    PrivateData = @{
        PSData = @{
            Tags = @(
                'JWT'
                'JSONWebToken'
                'JWS'
                'PowerShell'
            )
        }
    }
}